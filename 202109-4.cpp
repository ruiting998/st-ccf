#include<iostream>
#include<string>
using namespace std;
int main(){
	int n,k;
	cin>>n;
	double p[n];
	double num[n];
	memset(p,0,sizeof(p));
	memset(b,0,sizeof(b));
	for(int i=0;i<n;i++){
		cin>>a[i];
	}
	double tmp_p;
	for(int i=0;i<n;i++){
		tmp_p=p[i];
		for(int j=0;i<n;i++){
			
		}
	}
	return 0;
}
